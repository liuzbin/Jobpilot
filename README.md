# JobPilot

本地优先的求职助手：Chrome 插件 + 本地常驻 App（FastAPI + SQLite），不依赖任何云端服务器。当前进度见 `docs/DEVELOPMENT_LOG.md`（每个 Phase 的实现过程、踩坑与解决）和 `docs/JobPilot_实施方案.md`（分阶段实施计划的唯一依据）。下面这份 README 主要是"怎么把本地 App 和插件跑起来、怎么验收"的操作手册，按 Phase 0 的顺序写的，后面几个 Phase 新增的验证步骤追加在对应小节里。

## 目录结构

```
jobpilot/
├── backend/                     # 本地 App（Python / FastAPI）
│   ├── app/
│   │   ├── core/                # 配置、数据库连接、配对 token、自动迁移
│   │   ├── models/               # SQLAlchemy 表结构
│   │   ├── services/             # 画像/JD/打分/简历重制等业务逻辑
│   │   └── api/                  # REST + WebSocket 路由（routes_system 配对与插件专用接口，routes_dashboard 本地浏览器页面）
│   ├── alembic/                  # 数据库迁移脚本
│   └── tests/                    # pytest 自动化测试
└── extension/                    # Chrome 插件（Manifest V3）
    ├── background/                # service worker：连接状态机、心跳、转发插件抓取到的 JD 给本地 App、自动化填表相关的网络调用
    ├── content_scripts/           # 注入到 LinkedIn 职位页（Phase 3）和 Workday/Greenhouse/Lever 投递页（Phase 4）的脚本 + 对应的 jsdom 回归测试
    └── sidepanel/                 # 侧边栏 UI：配对、状态展示
```

## 一、启动本地 App

```bash
cd jobpilot/backend
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
python -m app.main
```

启动后会看到日志 `JobPilot local app ready. data dir = /Users/你的用户名/.jobpilot`，说明：

- 数据库文件、配对 token 都会自动创建在 `~/.jobpilot/` 目录下（Windows 下是用户目录的 `.jobpilot` 文件夹）。
- 本地服务只监听 `127.0.0.1:8756`，不会被局域网内其他设备访问到。
- 每次启动都会自动执行数据库迁移，不需要手动跑 `alembic upgrade head`。
- **Windows 用户会额外看到一行 `PDF 渲染依赖检查：...` 日志**：简历重制功能里"生成 PDF"这一步依赖 WeasyPrint，而 WeasyPrint 需要系统级的 GTK3 Runtime，`pip install -r requirements.txt` 装不出这个系统级依赖。这一步不需要你手动去找安装包——本地 App 每次启动时会自己探测，缺失时自动下载并调用 GTK3 Runtime 的安装程序，你只需要看这行日志里的提示：
  - 如果说"已就绪"或"当次即可使用"：不用做任何事。
  - 如果说"已自动安装完成，请重启一次本地 App"：按提示重新执行一次 `python -m app.main` 即可（这是 WeasyPrint 自身的限制——它探测系统依赖的逻辑只在进程刚启动时跑一次，装好之后必须换一个新进程才能生效，重启一次就好，不需要手动改任何 PATH/环境变量）。
  - **下载完成后会弹出一个 Windows 系统自己的管理员权限确认框**（"是否允许此应用对你的设备进行更改"），需要你点一下"是"才能继续往下装——GTK3 Runtime 默认装到 `C:\Program Files\`，这是系统保护起来的目录，装到这里必须要管理员权限，Windows 不允许任何程序不经用户同意就静默拿到这个权限，这一下确认没有办法省掉，是操作系统的安全机制决定的，不是本地 App 没做好自动化。点"是"之后的下载、安装本身仍然是全自动、不需要你再操心的。
  - 如果这个确认框你点了"否"，或者"自动安装失败"：日志里会附一个手动安装的链接，装完后重启本地 App。
  - Markdown/结构化数据这些简历重制的核心产物完全不受这个依赖影响，只有"下载 PDF"这一项功能会受影响。

## 二、运行自动化测试

```bash
cd jobpilot/backend
source .venv/bin/activate
python -m pytest -v
```

Phase 0 阶段是 12 条用例，覆盖数据库迁移和配对握手两类验收点；后面每个 Phase 都在原有基础上继续补充，覆盖范围随功能一起长（画像/JD 解析、打分引擎、简历重制、事实护栏校验、PDF 渲染、插件专用 API 等），具体每个 Phase 新增了什么测试、当时的用例总数是多少，见 `docs/DEVELOPMENT_LOG.md` 对应章节的"验证结果"小节——这里不再逐个 Phase 重复维护一份可能过时的数字，跑一遍上面的命令看实际输出的用例总数和是否全部通过就是最新的真实状态。

## 三、检查插件权限声明（无需 Chrome，命令行即可）

```bash
cd jobpilot/extension
node scripts/check_permissions.js
```

这个脚本会扫描插件代码里用到的 `chrome.*` API，对照 `manifest.json` 里声明的 `permissions`，提前发现"用了某个 API 但忘记在 manifest 里声明权限"这类问题（第一版就因为用了 `chrome.alarms` 却忘记声明 `alarms` 权限，导致后台脚本一启动就抛异常、侧边栏一直卡在"检测中"）。正常应该输出"权限检查通过"。

## 四、加载 Chrome 插件（需要你在自己电脑上手动完成）

这一步需要在你本机的真实 Chrome 里操作，云端环境没有图形界面无法代为完成，请按下面步骤验收：

1. 打开 `chrome://extensions`，右上角打开"开发者模式"。
2. 点击"加载已解压的扩展程序"，选择 `jobpilot/extension` 目录。
   - 如果你之前已经加载过旧版本，请点这个插件卡片上的"重新加载"（一个圆形箭头图标），**不要只刷新侧边栏页面**——`manifest.json` 权限变化只有重新加载整个插件才会生效。
3. 确认本地 App 正在运行（第一步）。
4. 点击浏览器工具栏里的 JobPilot 图标，应该会打开侧边栏。
5. 如果侧边栏一直卡在某个状态没反应，打开 `chrome://extensions`，找到 JobPilot 卡片上的"service worker"或"检查视图"链接，点进去看控制台有没有报错——这是插件侧最直接的排查手段，以后遇到类似问题可以先看这里。

### 验收清单

- [ ] 本地 App 未启动时，侧边栏显示"未连接"，并提示"还没检测到本地 App"。
- [ ] 启动本地 App 后（不用手动刷新），大约 1 分钟内或点击"重新检测"后，侧边栏状态变为"已检测到本地 App，待配对"。
- [ ] 点击"打开本地配对页面"，会新开一个标签页显示配对码（地址是 `http://127.0.0.1:8756/pair`）。
- [ ] 复制配对码粘贴到侧边栏输入框，点击"保存并连接"，状态应在几秒内变为"已连接"，并显示最近心跳时间，之后每 10 秒左右心跳时间会刷新一次。
- [ ] 手动关掉本地 App 进程（Ctrl+C），侧边栏应在心跳超时后（约 15-25 秒）变回"未连接"。
- [ ] 重新启动本地 App，侧边栏应自动重新连接（不需要重新输入配对码，因为 token 是持久化在两边的），除非你之前手动清过配对码或本地 App 的 `~/.jobpilot/pairing_token.json` 被删除重新生成过。
- [ ] 故意在侧边栏输入一个错误的配对码，应该看到"配对失败"提示，且不会无限重试同一个坏 token。

以上全部打勾即代表 Phase 0 验收通过，可以进入 Phase 1（端到端打分闭环：画像上传解析、JD 结构化、打分引擎、最简 Dashboard）。

## 五、抓取 LinkedIn 职位（Phase 3；发送按钮位置在打磨阶段调整过）

打开一个 LinkedIn 职位详情页，插件侧边栏里会出现一个"发送到 JobPilot"按钮，点一下就会把这条职位的公司、岗位、正文、地点、"X people clicked apply"这类附加信息一起发给本地 App，自动打开对应的 Dashboard 详情页——接下来的分析、简历重制流程和手动粘贴 JD 完全一样，走的是同一条入库/打分链路。

这个按钮最早是做成悬浮在页面右下角的（Phase 3 上线时的设计），后来发现 LinkedIn 是单页应用、会频繁大范围重渲染 DOM，悬浮按钮偶尔会被顶掉或者不出现，用户反馈"按钮有时候就是不出现"。打磨阶段把这个入口挪到了侧边栏里：侧边栏是插件自己的独立 UI，不受 LinkedIn 页面重渲染影响，检测逻辑变成"看当前激活的标签页网址是不是 LinkedIn 职位页面"——是的话侧边栏显示发送按钮，不是的话显示"未检测到职位信息"，不需要再去页面上找按钮。

### 不需要 Chrome 的回归测试

LinkedIn 的页面结构不受我们控制、也会不定期调整，抓取逻辑单独抽成一个不依赖浏览器的纯函数（`extension/content_scripts/linkedin_parser.js`），用 [jsdom](https://github.com/jsdom/jsdom) 模拟几种常见的 LinkedIn 页面布局（新版详情页、稍旧版、更旧的公开职位页、完全无法识别的布局）跑单元测试，可以在没有 Chrome、没有真实 LinkedIn 账号的情况下随时验证：

```bash
cd jobpilot/extension
npm install       # 第一次跑之前装一下开发依赖（只有 jsdom，运行插件本身不需要）
npm test          # 等价于 node content_scripts/linkedin_parser.test.js
```

正常应该看到 5 条用例全部 `ok`。如果哪天发现插件在真实 LinkedIn 页面上抓不到某个字段了，优先的排查方式是：把当时页面的 HTML 存一份到 `extension/content_scripts/__fixtures__/`，在 `linkedin_parser.test.js` 里加一条新用例复现问题，再去 `linkedin_parser.js` 里补一条新的选择器分支——而不是直接改代码之后祈祷它在真实页面上碰巧好使。

### 需要在真实 Chrome + 真实 LinkedIn 页面上手动验收的部分

云端环境没有图形界面、也没有 LinkedIn 账号，下面这些必须你在自己电脑上手动确认：

- [ ] 按上面"四、加载 Chrome 插件"重新加载一次插件（`manifest.json` 新增了 `content_scripts` 和 `host_permissions`，必须整个重新加载才会生效，不能只刷新侧边栏）。
- [ ] 在已连接状态下打开侧边栏，应该能看到"发送当前职位到 JobPilot"这张卡片。
- [ ] 依次访问几个不同的真实 LinkedIn 职位详情页（`linkedin.com/jobs/view/...`），每次切换标签页/刷新后，侧边栏应该都能正确变成"检测到职位页面，可以发送"的状态，不需要在页面上找悬浮按钮。
- [ ] 点击侧边栏的"发送到 JobPilot"按钮，按钮下方的状态文案应该依次变化："正在读取当前页面的职位信息…" → "发送中…" → "已发送，正在打开…"，同时自动新开一个标签页跳转到本地 Dashboard 对应 JD 的详情页，公司名、职位名、正文都应该和 LinkedIn 页面上看到的一致。
- [ ] 在 LinkedIn 的职位列表页里，不刷新页面、直接点击切换到另一个职位卡片，再点侧边栏的发送按钮，应该发送的是切换后这个新职位的信息（验证"单页应用切换职位不需要刷新页面"这条设计）。
- [ ] 打开一个明显不是职位详情页的 LinkedIn 页面（比如个人主页）或者切到别的网站，侧边栏应该自动变成"未检测到职位信息"的提示，看不到发送按钮。
- [ ] 故意在还没完成插件配对的状态下打开一个 LinkedIn 职位详情页：因为"发送当前职位到 JobPilot"这张卡片本身只在已连接状态下才显示，这时候侧边栏应该看到的是配对提示卡片，而不是一个点了会报错的发送按钮——这是比 Phase 3 悬浮按钮版本更严格的一个改进点，值得专门确认一下。

以上全部打勾即代表 Phase 3 验收通过。

## 六、自动化填表（Phase 4）

在已知的三家投递系统（Workday、Greenhouse、Lever）的投递页面上，插件会在页面右下角注入两个悬浮按钮："JobPilot 自动填表"和"附加简历 PDF"。点"自动填表"会扫描当前页面的表单字段，发给本地 App 算出每个字段该填什么，再把结果写回页面；点"附加简历 PDF"会把这条 JD 最近一次生成的简历 PDF 自动放进简历上传控件。另外，Dashboard 的 JD 详情页新增了"去投递"按钮：点击会在新标签页打开投递链接，同时让插件记住"接下来这个标签页对应这条 JD"，之后在投递页面上点击提交按钮，只要还是这个标签页，就会自动把这条 JD 的状态推进成"已投递"；如果你不是从这个按钮点过去（比如自己手动打开的投递页），插件不会做任何自动状态更新，你可以用详情页上的"手动标记为已投递"按钮自己同步状态。

**合规安全说明**：工作授权、签证担保、EEO 自报（性别/种族/退伍军人身份/残障状态）、犯罪记录/背景调查这类涉及法律声明性质的字段，插件不会自动帮你选（select/radio/checkbox 这类控件），需要你自己手动选择——这是有意为之的限制，不是 bug，详见 `docs/JobPilot_实施方案.md` 5.4 节的说明。

### 不需要 Chrome 的回归测试

和 Phase 3 的 LinkedIn 抓取一样，表单扫描和 ATS 平台识别的核心逻辑抽成了不依赖浏览器的纯函数，用 jsdom 做单元测试：

```bash
cd jobpilot/extension
npm install       # 第一次跑之前装一下开发依赖
npm test          # 依次跑 linkedin_parser / form_scanner / ats_selectors 三组测试
```

正常应该看到三组测试全部通过（`form_scanner.test.js` 16 条，覆盖各种标签关联方式和 select/radio/checkbox 字段提取；`ats_selectors.test.js` 9 条，覆盖平台识别和 Workday/Greenhouse/Lever 各自的标签解析规则）。后端的填表决策逻辑（`build_autofill_plan`）和合规安全限制在 `backend/tests/test_autofill.py` 里用 pytest 覆盖，随 `python -m pytest` 一起跑。

`ats_autofill.js`（悬浮按钮、DOM 填值、简历 PDF 注入、提交按钮监听）和 `dashboard_bridge.js`（"去投递"关联桥接）是纯浏览器胶水代码，依赖真实 DOM 事件和 `chrome.*` 消息通信，和 Phase 3 的 `linkedin.js` 一样无法用 jsdom 做有意义的单元测试，需要下面的手动验收。

如果哪天发现某个 ATS 平台上抓不到某个字段的标签、或者字段类型判断错了，优先的排查方式和 LinkedIn 抓取一样：把当时页面对应部分的 HTML 结构记录下来，在 `form_scanner.test.js` 或 `ats_selectors.test.js` 里加一条新用例复现问题，再去 `form_scanner.js`/`ats_selectors.js` 里补一条新的选择器/标签解析分支。

### 需要在真实 Chrome + 真实 ATS 页面上手动验收的部分

云端环境没有图形界面、也没有 Workday/Greenhouse/Lever 的真实测试账号，规则库是基于三家平台公开可观察到的约定构造的最佳努力实现，下面这些必须你在自己电脑上手动确认：

- [ ] 按上面"四、加载 Chrome 插件"重新加载一次插件（`manifest.json` 新增了 ATS 三家的 `host_permissions` 和 `content_scripts`，必须整个重新加载才会生效）。
- [ ] 找一个真实的 Workday/Greenhouse/Lever 投递页面打开，页面右下角应该能看到"JobPilot 自动填表"和"附加简历 PDF"两个悬浮按钮。
- [ ] 先在画像页面填好姓名/邮箱/电话/领英链接等基本信息，点击"JobPilot 自动填表"，按钮文案应该依次变化"扫描中…" → "计算填表方案中…" → "已填 N 个字段"，对应的输入框应该被正确填上；如果页面是 React/Vue 这类框架驱动的，确认填进去的值真的被框架感知到了（比如触发了页面自己的实时校验提示消失），而不是看起来填上了、提交时却读到空值。
- [ ] 找一个涉及"工作授权"/"是否需要担保"这类问题的单选/下拉字段，确认插件没有自动帮你选，需要你自己手动选择。
- [ ] 找一道需要主观作答的问答题（比如"为什么想加入我们"），如果题库里已经有相似的问题，确认能被自动填上；如果没有，自己写完答案后失去焦点，应该弹出"要不要存进题库"的确认框，确认后下次遇到类似问题能被检索到。
- [ ] 在 JD 已经生成过简历的情况下，点击"附加简历 PDF"，确认简历上传控件里出现了对应的 PDF 文件。
- [ ] 从 Dashboard JD 详情页点击"去投递"打开投递页，完成表单（不需要真的投递成功，找到提交按钮点一下即可，如果会触发真实投递请谨慎，可以用一个不重要的测试职位或者提交前及时关闭标签页），确认 Dashboard 对应 JD 的状态变成了"已投递"。
- [ ] 不通过"去投递"按钮、自己直接打开一个投递页面，点击提交按钮，确认没有任何 JD 状态被意外更新（验证"没有关联记录就完全不做任何事"这条设计）。
- [ ] 在 JD 详情页，点击"手动标记为已投递"，确认状态立刻变成"已投递"，按钮本身也随之消失。

以上全部打勾即代表 Phase 4 验收通过。

## 七、简历风格自定义（Phase 5）

生成简历时（"生成简历"确认页最下面）可以选一套排版风格，目前有两套：

- **默认（简洁单栏）**：项目符号列表，行距宽松，适合内容不算特别多的情况。
- **紧凑（更小间距，适合内容较多）**：更小的页边距和行距，去掉项目符号改成更紧凑的排布，同样内容能塞进更少的页数。

已经生成过的简历，不需要重新走一遍 K 值/延伸建议确认，可以直接在简历结果页底部换一套风格重新生成 PDF——这一步完全在本地渲染、不会再调用一次模型，所以可以随便换着对比效果。两套风格读的是同一份已经生成好的简历内容，换风格不会丢失或改变任何文字。

## 八、用量统计面板（Phase 5）

Dashboard 顶部导航新增"用量统计"页面，按轻量/重量两个槽位分别统计调用次数（含成功/失败）和累计 token 数，下面还有最近 50 条调用的明细（时间、模型、成功/失败、token 数、失败时的错误摘要）。

故意不做费用估算——每个人配置的模型服务商、套餐、汇率都不一样，本地 App 没办法可靠算出真实花费，算错了反而会误导人，具体花了多少钱请对照你自己那边模型服务商的账单，这里只提供最基础、可以确定不会算错的两个数字：调用次数和 token 数。

## 九、打包成独立可执行文件分发（Phase 5，可选）

前面几节的"启动本地 App"都要求先装 Python、建虚拟环境、`pip install`——如果你只是自己用，这样没问题；但如果想把这个本地 App 分享给不熟悉命令行的人用，可以打包成一个双击就能跑的独立可执行文件，不需要对方装 Python：

```bash
cd jobpilot/backend
pip install -r packaging/requirements-build.txt
pyinstaller packaging/jobpilot.spec
```

产出在 `backend/dist/JobPilot/` 目录下（Windows 上是这个目录里的 `JobPilot.exe`，双击运行；同目录下的其他文件是它运行所需要的依赖，整个 `JobPilot` 文件夹要一起分发，不能只拷贝 `.exe` 一个文件）。

**必须在目标系统上构建**：PyInstaller 不能跨平台编译，要给 Windows 用户分发 `.exe`，就必须在一台 Windows 机器上运行上面这两条命令；在 Windows 上打包出来的就是 Windows 能跑的 `.exe`，不需要额外处理 GTK3 的问题——打包只是把 Python 代码和这个本地 App 自己的依赖打包起来，GTK3 这种系统级依赖仍然要等实际运行的时候由 [Phase 4 补充] 里说的自动检测/安装逻辑处理，跟"有没有打包"是两件独立的事。

这一步在云端沙盒环境里只能验证打包配置本身对不对（模板、alembic 迁移脚本有没有正确随包带上，打包后的进程能不能正确找到这些文件、能不能正常跑数据库迁移、WeasyPrint 能不能正常渲染出 PDF），这几项都已经在 Linux 环境下构建并实际跑通验证过；真正要分发给 Windows 用户的 `.exe`，需要你在自己的 Windows 机器上按上面的步骤跑一遍。

**验证清单（Phase 5）：**

- [ ] 生成简历时，确认页最下面能看到风格选择下拉框，切换风格不影响前面已经选好的 K 值/延伸建议。
- [ ] 用"默认"风格生成一份简历，打开 PDF 确认是宽松间距、带项目符号的单栏排版。
- [ ] 在简历结果页底部把风格换成"紧凑"、点击"重新生成 PDF"，确认不用重新走 K 值/延伸建议确认，几秒内 PDF 就换成了更小间距、无项目符号的排版，且文字内容和换风格前完全一致。
- [ ] 重新生成 PDF 后刷新简历结果页，确认页面上显示的当前风格名称也跟着更新了。
- [ ] 打开 Dashboard 顶部导航"用量统计"页面：在还没调用过模型时应显示空状态（次数、token 数都是 0）；正常走一遍打分/生成简历的流程后再打开，轻量/重量两个槽位的调用次数、成功失败数、累计 token 数应该对得上，下面最近调用明细里能看到对应记录。
- [ ] 故意制造一次模型调用失败（比如把某个模型配置的 API Key 改错再触发一次打分），确认用量统计页面里这次调用被记成"失败"，并且明细里能看到错误摘要。
- [ ] 故意触发一个未被专门处理的异常（比如临时改错某个配置项），确认 Dashboard 页面展示的是友好的错误提示页而不是原始报错堆栈，命令行日志里能看到完整的异常堆栈方便排查。
- [ ] 【需要你在自己的 Windows 机器上验证，云端沙盒无法覆盖】按"一、启动本地 App"里的流程，在一台干净的 Windows 环境（未装过 GTK3）上首次启动本地 App，触发一次简历 PDF 生成，确认能看到自动检测/下载 GTK3 的日志提示，下载完成后会弹出 Windows 自己的管理员权限确认框，点"是"之后安装应该自动完成、不需要再手动操作；安装完成后 PDF 能正常生成，如果安装后需要重启 App 才能生效，确认提示文案讲清楚了这一点。也可以顺便验证一下点"否"取消授权的情况：应该看到一句能看懂的"授权被取消"提示，而不是难以理解的 Windows 错误码。
- [ ] 【需要你在自己的 Windows 机器上验证，云端沙盒无法覆盖】按"九、打包成独立可执行文件分发"里的命令，在 Windows 机器上实际跑一遍 PyInstaller 打包，确认生成的 `JobPilot.exe` 双击能正常启动本地 App、网页能正常打开、简历 PDF 能正常生成。

以上全部打勾即代表 Phase 5 验收通过，至此 Phase 0-5 全部完成。

## 十、打磨阶段用户反馈修复（GTK3 提权 / LLM 加载提示 / 发送按钮位置 / 职位合并模糊匹配）

Phase 0-5 全部完成之后，根据实际使用反馈又做了四处修复/改进，不算新 Phase，记在这里：

1. **GTK3 自动安装的 WinError 740**：见上面"一、启动本地 App"里更新过的说明——原来的自动安装用普通方式启动安装程序，遇到"需要管理员权限"的安装包直接报错不弹窗；现在改成主动弹出 Windows 的 UAC 授权确认框，用户点"是"就能继续，不再报错卡死。
2. **调用 LLM 时页面看起来卡住**：所有会调用 LLM 的按钮/链接（开始分析并打分、生成简历草稿、深化经历追问、生成常见问答题、上传简历解析）点击后会立刻显示一个带小圆圈动画的"AI 正在...，请稍候"提示、按钮同时禁用，等结果返回后随着整页刷新一起展示——没有引入后台任务队列，LLM 调用本身还是同步等待，只是不会再让人误以为页面卡死了。
3. **"发送到 JobPilot"按钮从页面悬浮按钮改成侧边栏常驻按钮**：LinkedIn 单页应用会频繁重渲染 DOM，悬浮按钮偶尔会被顶掉不出现；现在这个入口固定在插件侧边栏里，侧边栏检测当前标签页是不是 LinkedIn 职位页面，是的话显示可点击的发送按钮，不是的话显示"未检测到职位信息"，不用再去页面上找按钮。
4. **职位合并冲突检测扩展成模糊匹配**：原来"是不是同一段职位经历"只按标题+项目名标准化后精确匹配，改成精确匹配不到时再退一步模糊匹配——同一家公司、起止时间有重叠或一方没填、且标题/项目名文本相似度够高，判定为"很可能是同一段经历的不同措辞"，交给用户确认要不要合并，而不是直接当成一条新职位插入。全程只用字符串相似度算法（difflib），不调用 LLM。

**验证清单：**

- [ ] 【需要你在自己的 Windows 机器上验证，云端沙盒无法覆盖】按上面"启动本地 App"部分更新过的说明，验证 GTK3 自动安装现在会弹出 UAC 确认框、点"是"后能正常装完。
- [ ] 在 Chrome 里实际点一下"开始分析并打分""生成简历草稿""上传简历自动建画像"这几个按钮，确认点击后立刻能看到"AI 正在...，请稍候"的加载提示和小圆圈动画，而不是页面看起来没反应。
- [ ] 按"五、抓取 LinkedIn 职位"更新过的说明，验证侧边栏里的发送按钮能正确识别当前标签页是不是职位页面、能正常发送。
- [ ] 上传一份简历，里面某段经历和已有画像里的某段职位标题/项目名很相似但不完全一样（比如"后端工程师"改成"高级后端工程师"，时间区间有重叠），确认走的是合并冲突确认页面而不是直接多出一条职位；标题/项目名完全不像、或者时间对不上的两段经历，确认不会被误判成冲突。

## 十一、简历 MD 模板库 + 简历上传规则解析（打磨阶段用户反馈第 1/2 点）

用户提供了默认 MD 简历模板文件之后，落地了"十、打磨阶段用户反馈修复"里搁置的两项改动：

1. **简历风格新增"MD 模板"这一种类型，和原有的 `default`/`compact` 两套 CSS 风格并存**：CSS 风格仍然是"同一份结构化内容，套用不同的 HTML+CSS 排版"；MD 模板则是完全由你自己编写的 Jinja2 模板文本直接决定 Markdown 输出的样子，System 内置了一份根据你上传的示例简历整理出来的默认模板，你也可以在 Dashboard 顶部导航"简历模板"页面新增/编辑/删除自己的模板、指定其中一个为默认模板。生成简历、以及已经生成过的简历在结果页换风格重新生成 PDF，下拉框里都能在"内置风格"和"MD 模板"两个分组里二选一，换哪种都不需要重新走 K 值/延伸建议确认。
2. **简历上传解析：按 JobPilot 模板格式写的 `.md` 简历，改成不调用 LLM 的规则解析**：解析器识别"个人总结/技能/工作经历/独立项目/教育经历"这几个章节标题（支持中英文措辞的常见变体），用正则+字符串处理直接抽取结构化信息，确定性和速度都比 LLM 抽取更好。识别不出来任何一个章节标题的自由格式 `.md`、以及 PDF/Word/txt 这些其它格式，仍然走原来的 LLM 抽取兜底，两条路径产出的结构完全同构，Dashboard 这边不需要关心具体走的是哪一条。

同一批改动里，为了让 MD 模板能完整还原一份简历，画像新增了三块之前没有承载的内容，这几块内容明确定位为"静态背景信息"——原样写进每一版简历，不参与 JD 关键词匹配、不参与 K 值驱动的技能延伸判断，也不影响打分引擎：

- **个人总结 / 技能**：`profile.html` 基本信息表单新增两个文本域，也可以通过上传简历自动识别填入（只填空、不覆盖你已经手动确认过的内容，和其它基本信息字段的合并策略一致）。
- **教育经历**：新增独立的教育经历列表，支持多条记录，可以在画像页面手动增删改，也能从模板格式的简历里自动识别。
- **独立项目**：新增一个不挂靠任何公司的"独立项目"模块（比如课外项目、开源项目），每个项目下面可以挂多条贡献句，同样支持手动管理和自动识别。

教育经历、独立项目在从简历里自动识别合并时，用的是**精确匹配去重**（学校+学位完全一致、或者项目名完全一致，才认为是同一条记录），不像工作经历那样做模糊相似度匹配——这两类信息一般条目不多、也很少出现同一条记录被改写成不同措辞的情况，做模糊匹配收益不大，所以先按更简单的精确匹配来实现。如果以后发现有需要，可以再补上模糊匹配。

顺带修复了一个和这次改动无关、但读代码时发现的既有问题：内置的 `default`/`compact` 两套 CSS 风格原来只显示职位/项目名称和贡献句，完全不显示公司名称和时间区间——现在两套风格都补上了"公司名 · 职位/项目名"在左、时间区间在右的这一行。

**验证清单：**

- [ ] 打开 Dashboard 顶部导航"简历模板"页面，确认能看到一个内置的默认模板（基于你上传的示例简历整理而成），新增一个自定义模板、编辑已有模板的内容、把某个模板设为默认、删除一个非默认模板，确认都能正常工作；确认删除库里最后一个模板会被友好地拒绝（至少要保留一个）；确认新增/编辑模板时如果 Jinja2 语法写错了，会看到友好的错误提示而不是 500 报错。
- [ ] 在画像页面的基本信息表单里填写"个人总结"和"技能"两个文本域并保存，确认能正常保存和回显。
- [ ] 在画像页面新增一条教育经历、新增一个独立项目并给它加几条贡献句，确认都能正常增删改，删除项目时对应的贡献句也一起被删除。
- [ ] 按 JobPilot 模板格式（可以参考"简历模板"页面里默认模板的占位符说明）准备一份 `.md` 简历上传，确认画像里的姓名/联系方式/个人总结/技能/工作经历/独立项目/教育经历都被正确识别填入，而且这一次上传没有消耗任何 LLM 调用配额（可以对照"用量统计"页面的调用次数，上传前后应该没有变化）。
- [ ] 上传一份随手写的、不含任何可识别章节标题的自由格式 `.md`（或者一份 PDF/Word 简历），确认仍然能正常解析成功（这次会消耗一次轻量模型调用）。
- [ ] 生成一份简历时，把风格下拉框切换到"MD 模板"分组里的某个模板，确认生成的 Markdown/PDF 内容符合这个模板的排版；在简历结果页把风格换成另一个模板或者换回内置的 CSS 风格，确认不需要重新走 K 值/延伸建议确认。
- [ ] 用内置的"默认"或"紧凑"风格生成一份简历，打开 PDF 确认现在每段工作经历都显示了公司名称和时间区间（不再是只有职位/项目名和贡献句）。

## 十二、LinkedIn 画像导入（后端部分）

对照 LinkedIn 个人主页的真实板块结构，画像现在分成五块：**基本信息（含个人简介）/ Skills / 教育经历 / 工作经历 / 项目经历**。这一轮先落地了后端和 Dashboard 部分，**插件侧（侧边栏粘贴 LinkedIn 地址、导航抓取、发送到本地 App）还没做，是下一步**——在插件侧完成之前，下面这些新增能力需要手动调用 `POST /api/linkedin-profile` 接口（或者直接在 Dashboard 画像页手动填写）来验证。

1. **备注信息**：画像页顶部新增一个独立的"备注信息"文本框，填你自己对自己优势/劣势的评价、求职偏好等。这段内容**不会**出现在生成的简历正文里，但会被喂给 JD 匹配打分那次 LLM 调用，体现在分析结果的优劣势判断里。
2. **Skills（结构化技能标签）**：新增一个"Skills"板块，一个标签一个技能名（对照 LinkedIn Skills 板块的扁平结构），支持手动增删；原来"基本信息"表单里的自由文本"技能描述"字段完全保留，两者在生成简历时会合并展示（结构化标签里"技能描述文本没提到过的"会追加成新的一行，不重复）。
3. **项目经历关联公司标签**：独立项目（现在改叫"项目经历"）新增一个"关联公司"自由文本标签，用来人工标出"这个项目其实是在某家公司实习/工作期间做的"，只在项目这一侧加，不会修改对应公司经历的内容——两边各自保留独立的描述。
4. **`POST /api/linkedin-profile` 接口**：走和插件抓 JD 一样的配对鉴权，payload 结构和简历上传解析出来的结构同构（公司/职位/贡献句、教育经历、独立项目字段名完全一样），额外多了 `skills`（字符串数组）和 `projects[].company_tag` 两个字段，合并逻辑完全复用简历上传那一套（模糊匹配、精确去重规则不变）。

**验证清单：**

- [ ] 在画像页面顶部的"备注信息"文本框里填一段自我评价并保存，确认能正常保存和回显；跑一次"分析 JD"，确认分析出来的优劣势里能体现出这段备注信息的内容。
- [ ] 在"Skills"板块手动新增几个技能标签，确认能正常增删；重复新增同一个技能名（大小写不同也算重复）确认会被友好拒绝。
- [ ] 在"项目经历"里给某个独立项目填一个"关联公司"标签并保存，确认画像页面能看到这个标签，且没有影响项目名称、时间等其它字段。
- [ ] 用 `curl`（或者 Postman）带上配对 token，往 `POST http://127.0.0.1:8756/api/linkedin-profile` 发一份模拟的 LinkedIn 画像 JSON（可以参考 `backend/tests/test_routes_extension.py` 里新增的 LinkedIn 相关用例），确认返回 `dashboard_url` 且画像页面能看到对应的技能/教育经历/工作经历/项目经历都被正确合并进去，重复发送同一份数据不会产生重复记录。

## 十三、打磨阶段用户反馈修复：JD 分析提速 + LinkedIn 抓取保留 JD 排版（2026-09-15）

1. **JD 分析改用纯轻量模型**：之前"分析 JD"这一步（结构化解析 + 技能契合度打分）打分那部分走的是重量模型槽位，两次调用顺序执行、且打分这步 prompt 偏大，导致明显延迟。确认后改成两步都只依赖轻量模型槽位——"模型配置"页面现在只需要填轻量模型就能跑通 JD 分析；重量模型继续只用于简历重制阶段的创造性改写（重组贡献句、生成技能延伸建议），不受这次改动影响。
2. **LinkedIn 抓取保留 JD 正文的段落/列表结构**：之前插件抓 LinkedIn 职位描述时，会把原文里 `<p>`/`<li>` 分隔的段落和条目全部压缩成一整行，导致 Dashboard 页面看到的 JD 正文挤成一坨、看不出结构。现在按块级标签（段落/列表项/换行/标题等）保留换行，行内的加粗/链接等标签不会拆碎句子；单纯是一段连续文本（没有段落/列表结构）时行为和以前完全一样。

**验证清单：**

- [ ] 在"模型配置"页面只填轻量模型（不填重量模型），对一条待分析的 JD 点击"开始分析并打分"，确认能正常跑通并出结果；对比改动前，明显感觉更快。
- [ ] 用插件在一个真实的 LinkedIn 职位详情页上点击抓取，确认 Dashboard 上看到的 JD 正文能看出原有的段落/项目符号结构，而不是所有文字挤在一起；已经存量抓取过的旧 JD 记录不受影响（这次改动只影响新抓取的数据）。
- [ ] 跑一次生成定制简历（K 值随意），确认这一步用的仍然是重量模型、生成质量没有变化。

## 安全说明

- 配对 token 保存在本地文件 `~/.jobpilot/pairing_token.json`，不要把这个文件或其中的内容分享给别人。
- 本地服务的所有需要鉴权的接口都同时校验"配对 token"和"请求来源（Origin 必须是 `chrome-extension://` 开头）"，防止你打开的其他网页伪装成插件向本地 App 发请求。
- 目前 Origin 校验只要求前缀是 `chrome-extension://`，尚未锁定到具体插件 ID；插件正式打包发布、拿到固定 ID 后，建议通过环境变量 `JOBPILOT_ALLOWED_ORIGIN` 把它锁死到那一个具体来源，这个会在后续打磨阶段处理。
